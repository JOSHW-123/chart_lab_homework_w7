import React, {useState, useEffect} from 'react';
// Import other components & css later.

const ChartContainer = () => {
    const [charts, setCharts] = useState([]);
    const [selectedChart, setSelectedChart] = useState(null);
}